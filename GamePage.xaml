<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="TrafficEscapeSimulator.GamePage"
             Title="GamePage">
    <VerticalStackLayout
                Padding="30,0"
                Spacing="25">
        <Grid x:Name="MainGrid" RowDefinitions="43,577,Auto" BackgroundColor="Grey">
            <!--<Image Source="topstad.png" ZIndex="-2" Aspect="AspectFill" Grid.Row="0"/>-->
            <!-- Score Display Area -->
            <Grid 
                    Grid.Row="0" 
                    ColumnDefinitions="*,*,*" 
                    Margin="0,10" Padding="10,0">
                <!-- Best Score -->
                <HorizontalStackLayout HorizontalOptions="Start" Grid.Column="0">
                    <Label 
                             Text="Best:  " 
                             FontSize="16" 
                             FontAttributes="Bold"
                             TextColor="White"/>
                    <Label 
                             x:Name="BestScoreLbl"
                             Text="0"
                             FontSize="16" 
                             FontAttributes="Bold"
                             TextColor="White"/>
                </HorizontalStackLayout>
                <!-- Score -->
                <HorizontalStackLayout HorizontalOptions="Center" Grid.Column="1">
                    <Label  
                                Text="Score:  " 
                                FontSize="18" 
                                FontAttributes="Bold"
                                TextColor="Yellow"/>
                    <Label 
                                x:Name="ScoreLbl" 
                                Text="0"
                                FontSize="18" 
                                FontAttributes="Bold"
                                TextColor="Yellow"/>
                </HorizontalStackLayout>
                <!-- Timer -->
                <HorizontalStackLayout HorizontalOptions="End" Grid.Column="2">
                    <Label 
                               Text="Time: " 
                               FontSize="16" 
                               FontAttributes="Bold"
                               TextColor="White"/>
                    <Label 
                                x:Name="timer_lbl"
                                Text="30"
                                FontSize="16" 
                                FontAttributes="Bold"
                                TextColor="White"/>
                </HorizontalStackLayout>
            </Grid>
            
            <!-- Game Area -->
            <!-- This is the main space that gameplay will take place -->
            <Grid 
                Grid.Row="1" 
                x:Name="GameArea"
                ColumnDefinitions="*, *, *"
                ColumnSpacing="10">
                <!-- Player's Car (Starts in Center)-->
                <!-- made redunandt with the discorvery of generating elements in c#
                <Image 
                    Grid.Column="1"
                    x:Name="PlayerCar" 
                    Source="car.png"
                    WidthRequest="128"
                    Aspect="Fill"
                    HeightRequest="128"
                    VerticalOptions="Center"
                    HorizontalOptions="Center"
                    Margin="0,100,0,0"
                    ZIndex="1"/>-->
                
                <!-- Road/Car Lanes Area (Where cars will be moving) -->
                <Grid Grid.Column="0" x:Name="Lane1">
                    <Image 
                           x:Name="Lane1Car1" 
                           Source="audi.png" 
                           WidthRequest="128"
                           Aspect="Fill"
                           HeightRequest="128"
                           VerticalOptions="End"
                           HorizontalOptions="Center"
                           Margin="0,100,0,0"
                           IsVisible="False"
                           ZIndex="1"/>

                    <Image 
                           x:Name="Lane1Car2" 
                           Source="blackviper.png" 
                           WidthRequest="128"
                           Aspect="Fill"
                           HeightRequest="128"
                           VerticalOptions="End"
                           HorizontalOptions="Center"
                           Margin="0,100,0,0"
                           IsVisible="False"
                           ZIndex="1"/>


                </Grid>

                <Grid Grid.Column="1" x:Name="Lane2">
                    <Image 
                           x:Name="Lane2Car1" 
                           Source="taxi.png" 
                           WidthRequest="128"
                           Aspect="Fill"
                           HeightRequest="128"
                           VerticalOptions="End"
                           HorizontalOptions="Center"
                           Margin="0,100,0,0"
                           IsVisible="False"
                           ZIndex="1"/>

                    <Image 
                               x:Name="Lane2Car2" 
                               Source="minitruck.png" 
                               WidthRequest="128"
                               Aspect="Fill"
                               HeightRequest="128"
                               VerticalOptions="End"
                               HorizontalOptions="Center"
                               Margin="0,100,0,0"
                               IsVisible="False"
                               ZIndex="1"/>


                    

                </Grid>
                <Grid Grid.Column="2" x:Name="Lane3">
                    <Image 
                        x:Name="Lane3Car1" 
                        Source="blackviper.png" 
                        WidthRequest="128"
                        Aspect="Fill"
                        HeightRequest="128"
                        VerticalOptions="End"
                        HorizontalOptions="Center"
                        Margin="0,100,0,0"
                        IsVisible="False"
                        ZIndex="1"/>

                    <Image 
                               x:Name="Lane3Car2"
                               Source="ambulance.png" 
                               WidthRequest="128"
                               Aspect="Fill"
                               HeightRequest="128"
                               VerticalOptions="End"
                               HorizontalOptions="Center"
                               Margin="0,100,0,0"
                               IsVisible="False"
                               ZIndex="1"/>
                    
                </Grid>
                
            </Grid>
            
                <!-- Feedback Label (for 3s countdown at beginning of game)
                <Label 
                               x:Name="FeedbackLbl"
                               Text="Goal Scored" 
                               FontSize="48"
                               FontAttributes="Bold"
                               TextColor="Yellow"
                               HorizontalOptions="Center"
                               VerticalOptions="Center"
                               IsVisible="False"
                               ZIndex="3"/> -->
        

            <Button 
                            x:Name="StartBtn" 
                            Text="Start Game" 
                            Grid.Row="2"
                            FontSize="16"
                            Padding="20,10"
                            BackgroundColor="Green"
                            TextColor="White"
                            HorizontalOptions="Center"
                            Clicked="StartBtn_Clicked"/>
            <!-- Control Buttons -->
            <Grid 
                            x:Name="ControlGrid" Grid.Row="2" 
                            ColumnDefinitions="*,*,*" 
                            ColumnSpacing="10" IsVisible="False">
                <Button 
                            Text="Left" 
                            Grid.Column="0"
                            FontSize="16"
                            BackgroundColor="Red"
                            TextColor="White"
                            Clicked="ShootButton_Clicked"/>

                    <Button
                            Text="Centre" 
                            Grid.Column="1"
                            FontSize="16"
                            BackgroundColor="Red"
                            TextColor="White"
                            Clicked="ShootButton_Clicked"/>

                <Button 
                            Text="Right" 
                            Grid.Column="2"
                            FontSize="16"
                            BackgroundColor="Red"
                            TextColor="White"
                            Clicked="ShootButton_Clicked"/>
            </Grid>
        </Grid>
    </VerticalStackLayout>
</ContentPage>
<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="TrafficEscapeSimulator.Options"
             Title="Options">
    <VerticalStackLayout>
        <Label 
            Text="Difficulty"
            FontSize="32" 
            FontAttributes="Bold"
            VerticalOptions="Center" 
            HorizontalOptions="Center" />
        <HorizontalStackLayout
            HorizontalOptions="Center">
            <Button
                    x:Name="NormalBtn" 
                    Text="Normal" 
                    FontSize="21"
                    Padding="20,10"
                    Margin="20"
                    BackgroundColor="Green"
                    TextColor="White"
                    Clicked="Difficulty"
                    IsEnabled="False"/>
            
            <Button
                x:Name="HardBtn"
                Text="Hard" 
                FontSize="21"
                Padding="20,10"
                Margin="20"
                BackgroundColor="Green"
                TextColor="White"
                Clicked="Difficulty"/>
            
        </HorizontalStackLayout>
    </VerticalStackLayout>
</ContentPage>
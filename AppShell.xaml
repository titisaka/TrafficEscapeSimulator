<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="TrafficEscapeSimulator.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:TrafficEscapeSimulator"
    Title="TrafficEscapeSimulator">

    <ShellContent
        Title="Traffic Escape"
        ContentTemplate="{DataTemplate local:MainPage}"
        Route="MainPage"/>
        <!-- isVisable as false has prevented the use of the back button
             a weird solution, but a solution nonetheless 
             will try to find something a bit more elegant later -->
    <!-- DO NOT TOUCH -->
</Shell>
